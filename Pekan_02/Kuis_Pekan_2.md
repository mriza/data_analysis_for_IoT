# Kuis Pekan 2: Setup Lingkungan dan Message Broker (MQTT)

Jawablah pertanyaan-pertanyaan berikut dengan jelas dan ringkas!

1. Saat membangun Virtual Machine (VM) di VirtualBox untuk simulasi server IoT, alasan utama disarankannya penggunaan kombinasi jaringan **NAT dan Host-Only Adapter** secara bersamaan adalah:

2. Perintah bawaan pada Windows 10/11 yang dapat digunakan untuk masuk (login) ke server Ubuntu secara jarak jauh tanpa perlu menginstal aplikasi pihak ketiga seperti PuTTY adalah:

3. Dalam protokol komunikasi MQTT, perangkat yang bertugas mengirimkan data sensor (misal: mikrokontroler) dikenal dengan istilah:

4. Secara default (standar internasional), komunikasi protokol MQTT beroperasi pada port berapa?

5. Jika RabbitMQ dan Mosquitto dijalankan secara bersamaan pada *server* yang sama dengan *plugin* MQTT aktif di keduanya tanpa ada port yang diubah, apa yang akan terjadi?

6. Perintah `sudo systemctl disable mosquitto` digunakan untuk:

7. Pada konfigurasi keamanan Mosquitto (`mosquitto.conf`), parameter apa yang harus diatur menjadi `false` agar *broker* menolak koneksi dari perangkat yang tidak memasukkan *username* dan *password*?

8. Dalam arsitektur broker pesan RabbitMQ, komponen yang bertugas **menerima pesan dari publisher dan memutuskan (routing) ke mana pesan tersebut harus diarahkan** disebut:

9. Pada praktikum *setup* lingkungan Python, sangat disarankan menggunakan perintah `python -m venv env_iot` sebelum menginstal *library* (seperti pandas dan paho-mqtt). Apa tujuan utama pembuatan *virtual environment* ini?

10. *Library* (modul) Python standar yang digunakan dalam praktikum untuk menghubungkan *script publisher* Python ke MQTT Broker Mosquitto adalah:
